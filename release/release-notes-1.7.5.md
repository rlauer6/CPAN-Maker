# CPAN::Maker Release Notes — v1.7.5

## Overview

A small patch release. One functional fix to `make-cpan-dist` to
add `Makefile$` to skip list. Fixes exclusion of `Makefile.PL` from
the distribution.

---

## Bug Fixes

### `make-cpan-distl` - add `Makefile$` to `MANIFEST.SKIP`
